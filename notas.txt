    else if(checkbox2){
      error.style.display= "none";
      monto += 5000;
      cotizacion.innerHTML= 'Su cotización es: ' + monto
        return
    }
    else if(checkbox3){
      error.style.display= "none";
      monto += 7500;
      cotizacion.innerHTML= 'Su cotización es: ' + monto
      return
    }
    else if(checkbox4){
      error.style.display= "none";
      monto += 8000;
      cotizacion.innerHTML= 'Su cotización es: ' + monto
      return
   }
   else if(checkbox5){
    error.style.display= "none";
    monto += 5000;
    cotizacion.innerHTML= 'Su cotización es: ' + monto
    return 
  }
  else if(checkbox6){
    error.style.display= "none";
    monto += 6000;
    cotizacion.innerHTML= 'Su cotización es: ' + monto
    return

      return 
    } 
    else{
      error.innerHTML='*Debe seleccionar una aventura';
    }
}

